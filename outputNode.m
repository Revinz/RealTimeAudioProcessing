classdef OutputNode < Node
    methods
        
        function playBuffer(obj)
            
            %Play the buffer
            
            
            %Pass it on
            
            
        end
        
        function applyEffect(obj, buffer)
            nodeName = obj.Name
            obj.playBuffer(obj);
            
        end
    end
end